{
  "framework": "nextjs",
  "buildCommand": "pnpm install --network-timeout 300000 --no-frozen-lockfile && pnpm run build",
  "installCommand": "pnpm install --network-timeout 300000 --no-frozen-lockfile",
  "devCommand": "pnpm run dev",
  "outputDirectory": ".next",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 60
    }
  }
}