"use client";

// Stub reexport to maintain i18n compatibility
// Routes the [locale]/token/[address]/[id] to the main token page
import TokenPage from '@/app/token/[address]/[id]/page';

export default TokenPage;