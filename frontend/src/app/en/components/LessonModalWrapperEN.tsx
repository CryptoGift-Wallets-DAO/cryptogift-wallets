"use client";

// English wrapper for LessonModalWrapper
// This wrapper uses the Spanish component but could be extended with English translations

import { LessonModalWrapper as SpanishLessonModalWrapper } from '../../../components/education/LessonModalWrapper';

export const LessonModalWrapperEN = SpanishLessonModalWrapper;

// In the future, this could be extended to:
// 1. Override text content with English translations
// 2. Pass locale prop to the original component
// 3. Use English-specific lesson content

// For now, it uses the same component to maintain full functionality
// The Sales Masterclass content is already bilingual-ready